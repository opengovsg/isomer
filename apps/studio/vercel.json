{
  "regions": ["sin1"],
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../../packages/components && pnpm run build && cd - && pnpm run migrate && pnpm run migrate:custom && SKIP_ENV_VALIDATION=true pnpm run db:seed && pnpm run build:preview-tw && pnpm run build"
}
