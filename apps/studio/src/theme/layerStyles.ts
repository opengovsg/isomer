import { type SystemStyleInterpolation } from "@chakra-ui/react"

export const layerStyles = {
  post: {
    _hover: {
      bg: "interaction.muted.main.hover",
    },
  },
} satisfies Record<string, SystemStyleInterpolation>
