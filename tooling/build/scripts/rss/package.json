{
  "name": "rss",
  "version": "1.0.0",
  "description": "Generates RSS 2.0 feeds for collections during the static site build.",
  "license": "ISC",
  "main": "index.ts",
  "scripts": {
    "start": "tsx index.ts",
    "test": "vitest run",
    "test-ci:unit": "vitest run",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "@opengovsg/isomer-components": "workspace:*",
    "date-fns-tz": "catalog:",
    "tsx": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "vitest": "catalog:vitest"
  }
}
